.container
  - if @foods.present?
    .section-title.text-center
      %h2 サ飯
    /投稿ボタン
    = render "new-btn"
    -# 検索フォーム
    = render 'foods/following_search_form', q_following: @q_following
    
    -# 投稿一覧
    .cards-list
      .cards-list__tab.flex-box
        .cards-list__tab__item
          .cards-list__tab__inactive
            = link_to 'みんなの投稿', foods_path
        - if user_signed_in?
          .cards-list__tab__item
            .cards-list__tab__active
              = link_to 'お気に入りの投稿', following_foods_path
        - else
          .cards-list__tab__item
            .cards-list__tab__inactive
              %span お気に入りの投稿
    = render 'foods_list', foods: @foods
  - else
    %h2 まだ投稿がありません
    