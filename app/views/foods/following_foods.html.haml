.container
  - if @foods.present?
    .section-title.text-center
      %h2 サ飯
    /投稿ボタン
    = render "new-btn"
    -# 検索フォーム
    = render 'foods/following_search_form', q_following: @q_following
    
    -# 投稿一覧
    = render 'following_foods_list', foods: @foods
  - else
    %h2 まだ投稿がありません
    